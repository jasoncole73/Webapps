// Rename this file to `config.js` and put your real OpenWeatherMap API key below.
// IMPORTANT: Client-side keys are visible to users. To keep it private, do not commit `config.js` to GitHub.
// Add `config.js` to your .gitignore. On GitHub Pages, anyone can view client JS, so treat this key as public.
window.WEATHER_API_KEY = "YOUR_OPENWEATHERMAP_API_KEY_HERE";
